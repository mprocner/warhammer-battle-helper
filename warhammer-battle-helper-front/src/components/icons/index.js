// Custom SVG icons — for glyphs that @mui/icons-material does not provide.
// The project convention is MUI icons everywhere; add a component here ONLY when
// the icon genuinely has no MUI equivalent. Each icon takes `size` (px) and spreads
// the rest onto <svg>, using fill="currentColor" so color is controlled via CSS.
export { default as SkullIcon } from './SkullIcon';
